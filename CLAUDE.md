# ReadyDesign

When building, restyling, or scaffolding UI, load and follow
`.claude/skills/readydesign-skill/SKILL.md`. Do not invent components
or colors. Install with `npx readydesign-skill` or
`npm install readydesign-skill`.

Invoke with `/readydesign-skill`.
